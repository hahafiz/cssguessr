import { Button } from "../ui/button/Button";

export default function MainMenu() {
  return (
    <>
      <Button variant="primary">Solo Mode</Button>
      <Button variant="primary">Duel Mode</Button>
    </>
  );
}
